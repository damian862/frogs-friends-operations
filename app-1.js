const sb=supabase.createClient('https://euxgcqkhsubxhuxhtrev.supabase.co','sb_publishable_rrACPD0dKGGxRuZjSoJfFw_Y6DBKFIB',{auth:{detectSessionInUrl:false,lock:async(_name,_timeout,fn)=>await fn()}});const $=x=>document.getElementById(x);let P,S=[],H=[],T=[],B=[],Y=[],D=[],G=[],BR=[],RS=[],EX=[];const e=x=>String(x??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const sn=id=>S.find(x=>x.id===id)?.name||'',hn=id=>H.find(x=>x.id===id)?.name||'',yn=id=>Y.find(x=>x.id===id)?.name||'';
function showMsg(t,bad=false){$('msg').innerHTML='<div class="'+(bad?'err':'note')+'">'+e(t)+'</div>'}async function login(){try{showMsg('Signing in…');let{data,error}=await sb.auth.signInWithPassword({email:$('email').value.trim(),password:$('password').value});if(error)return showMsg(error.message,true);await enter(data.user)}catch(x){showMsg(x.message||String(x),true)}}async function enter(u){let{data,error}=await sb.from('profiles').select('*').eq('id',u.id).single();if(error)return showMsg('Profile error: '+error.message,true);P=data;$('auth').classList.add('hide');$('app').classList.remove('hide');$('who').textContent=P.full_name||P.email;$('role').textContent=(P.role||'').replaceAll('_',' ');await load()}async function load(){let r=await Promise.all([sb.from('sites').select('*').order('name'),sb.from('hirers').select('*').order('name'),sb.from('staff').select('*').order('last_name'),sb.from('bookings').select('*').order('booking_date',{ascending:false}).limit(500),sb.from('academic_years').select('*').order('starts_on',{ascending:false}),sb.from('academic_calendar_periods').select('*').order('starts_on'),sb.from('recurring_programmes').select('*').order('name'),sb.from('recurring_programme_breaks').select('*').order('starts_on'),sb.from('recurring_programme_sessions').select('*').order('day_of_week').order('start_time'),sb.from('recurring_programme_session_exceptions').select('*').order('exception_date')]);[S,H,T,B,Y,D,G,BR,RS,EX]=r.map(x=>x.data||[]);render()}
function render(){$('k1').textContent=S.length;$('k2').textContent=T.length;$('k3').textContent=H.length;$('k4').textContent=B.length;$('rSites').innerHTML=S.map(x=>`<tr><td>${e(x.name)}</td><td>${e(x.town)}</td><td>${e(x.postcode)}</td><td><button class=link onclick="editSite('${x.id}')">Edit</button></td></tr>`).join('');$('rHirers').innerHTML=H.map(x=>`<tr><td>${e(x.name)}</td><td>${e(x.billing_email)}</td><td>${e(x.phone)}</td><td>${x.vat_registered?'Yes':'No'}</td><td>${canManageHirers()?`<button class=link onclick="editHirer('${x.id}')">Edit</button> · <button class=link onclick="deleteHirer('${x.id}')">Delete</button>`:''}</td></tr>`).join('');$('rStaff').innerHTML=T.map(x=>`<tr><td>${e(x.first_name+' '+x.last_name)}</td><td>${e(x.primary_role)}</td><td>${e((x.employment_status||'').replaceAll('_',' '))}</td><td>${e(x.email)}</td><td><button class=link onclick="editStaff('${x.id}')">Edit</button></td></tr>`).join('');renderBookingTables();renderTermDates();renderRecurringBookings();renderIncomeSummary()}function bookingHours(x){let a=String(x.start_time||'00:00').split(':').map(Number),b=String(x.end_time||'00:00').split(':').map(Number);return Math.max(0,(b[0]*60+b[1]-a[0]*60-a[1])/60)}function sessionHours(start,end){let a=String(start||'00:00').split(':').map(Number),b=String(end||'00:00').split(':').map(Number);return Math.max(0,(b[0]*60+b[1]-a[0]*60-a[1])/60)}
function bookingChargeHtml(x){if(x.charge_type==='internal_school_use')return '<span class="charge-badge internal">Internal school use</span>';if(x.charge_type==='free_of_charge')return '<span class="charge-badge foc">Free of charge</span>';if(x.rate==null)return '<span class="charge-badge">Rate not set</span>';let v=Number(x.rate)*bookingHours(x);return `<strong>£${v.toFixed(2)}</strong><div class="muted">£${Number(x.rate).toFixed(2)}/hr · ${x.vat_applicable?'VAT applies':'No VAT'}</div>`}
function bookingOrg(x){return hn(x.hirer_id)||'School/Internal'}
function renderBookingTables(){let row=x=>`<tr><td>${e(ukDate(x.booking_date))}</td><td><b>${e(bookingOrg(x))}</b></td><td>${e(x.title)}</td><td>${e(sn(x.site_id))}</td><td>${String(x.start_time||'').slice(0,5)}–${String(x.end_time||'').slice(0,5)}</td><td>${bookingChargeHtml(x)}</td><td><button class=link onclick="editBooking('${x.id}')">Edit</button></td></tr>`;if($('rBookings'))$('rBookings').innerHTML=B.map(row).join('')||'<tr><td colspan="7" class="muted">No bookings yet.</td></tr>';if($('rSingleBookings'))$('rSingleBookings').innerHTML=B.filter(x=>x.booking_type!=='school_internal').map(row).join('')||'<tr><td colspan="7" class="muted">No single bookings yet.</td></tr>';if($('rSchoolBookings'))$('rSchoolBookings').innerHTML=B.filter(x=>x.booking_type==='school_internal').map(x=>`<tr><td>${e(ukDate(x.booking_date))}</td><td>${e(x.title)}</td><td>${e(sn(x.site_id))}</td><td>${String(x.start_time||'').slice(0,5)}–${String(x.end_time||'').slice(0,5)}</td><td>${e(x.status||'confirmed')}</td><td><button class=link onclick="editBooking('${x.id}')">Edit</button></td></tr>`).join('')||'<tr><td colspan="6" class="muted">No school events yet.</td></tr>'}
function renderIncomeSummary(){let host=$('incomeSummary');if(!host)return;let confirmed=B.filter(x=>x.status!=='cancelled'),hours=confirmed.reduce((a,x)=>a+bookingHours(x),0),chargeable=confirmed.filter(x=>(x.charge_type||'chargeable')==='chargeable'),net=chargeable.reduce((a,x)=>a+(x.rate==null?0:Number(x.rate)*bookingHours(x)),0),foc=confirmed.filter(x=>x.charge_type==='free_of_charge'||x.charge_type==='internal_school_use').reduce((a,x)=>a+bookingHours(x),0);host.innerHTML=`<div class="income-grid"><div class="income-card">Total booked hours<b>${hours.toFixed(1)}</b></div><div class="income-card">Chargeable bookings<b>${chargeable.length}</b></div><div class="income-card">Booked net hire income<b>£${net.toFixed(2)}</b></div><div class="income-card">FOC / school hours<b>${foc.toFixed(1)}</b></div></div><div class="note" style="margin-top:12px">Monthly targets and recurring-booking forecasts are stored in the database and will be connected to this reporting view next.</div>`}
function setBookingTab(name){document.querySelectorAll('.booking-panel').forEach(x=>x.classList.remove('on'));document.querySelectorAll('.booking-tab').forEach(x=>x.classList.remove('active'));let m={all:'bookingTabAll',recurring:'bookingTabRecurring',single:'bookingTabSingle',school:'bookingTabSchool',income:'bookingTabIncome'};$(m[name]||m.all)?.classList.add('on');document.querySelector(`.booking-tab[data-btab="${name}"]`)?.classList.add('active');if(name==='recurring')renderRecurringBookings();if(name==='income')renderIncomeSummary()}
document.addEventListener('click',ev=>{let b=ev.target.closest('.booking-tab');if(b)setBookingTab(b.dataset.btab)});
let OPEN_PROG=null;
function recurringFilterOptions(){let site=$('rbSite'),org=$('rbOrg');if(site&&site.options.length===0){site.innerHTML='<option value="">All sites</option>'+S.map(x=>`<option value="${x.id}">${e(x.name)}</option>`).join('')}if(org&&org.options.length===0){org.innerHTML='<option value="">All organisations</option><option value="internal">School/Internal</option>'+H.map(x=>`<option value="${x.id}">${e(x.name)}</option>`).join('')}}
function resetRecurringFilters(){if($('rbSearch'))$('rbSearch').value='';if($('rbSite'))$('rbSite').value='';if($('rbOrg'))$('rbOrg').value='';if($('rbStatus'))$('rbStatus').value='active';renderRecurringBookings()}
function manageProgramme(id){OPEN_PROG=OPEN_PROG===id?null:id;renderRecurringBookings()}
function renderRecurringBookings(){let host=$('rProg');if(!host)return;recurringFilterOptions();let days=['Sun','Mon','Tue','Wed','Thu','Fri','Sat'];let q=($('rbSearch')?.value||'').trim().toLowerCase(),site=$('rbSite')?.value||'',org=$('rbOrg')?.value||'',status=$('rbStatus')?.value||'active';let rows=G.filter(x=>{let active=x.active!==false;if(status==='active'&&!active)return false;if(status==='archived'&&active)return false;if(site&&x.site_id!==site)return false;if(org==='internal'&&x.hirer_id)return false;if(org&&org!=='internal'&&x.hirer_id!==org)return false;if(q&&!`${x.name||''} ${sn(x.site_id)} ${hn(x.hirer_id)||'school internal'}`.toLowerCase().includes(q))return false;return true}).sort((a,b)=>(sn(a.site_id)+hn(a.hirer_id)+(a.name||'')).localeCompare(sn(b.site_id)+hn(b.hirer_id)+(b.name||'')));
host.innerHTML=rows.map(x=>{let sessions=RS.filter(r=>r.programme_id===x.id&&r.active!==false),breaks=BR.filter(r=>r.programme_id===x.id),ex=EX.filter(r=>r.programme_id===x.id),open=OPEN_PROG===x.id,active=x.active!==false;let manage='';if(open){let sessionRows=sessions.length?sessions.map(r=>{let hrs=sessionHours(r.start_time,r.end_time),ct=r.charge_type||'chargeable',total=r.rate==null?null:Number(r.rate)*hrs;return `<div class="term-row"><div class="term-label">${e(days[r.day_of_week]||'Day')} ${String(r.start_time||'').slice(0,5)}–${String(r.end_time||'').slice(0,5)}<div class="muted">${e((r.title||'Swimming lessons').trim())}<div class="muted">${r.pool_use_type==='lanes'?(r.lane_count+' lane'+(Number(r.lane_count)===1?'':'s')):(r.pool_use_type==='other'?'Other pool allocation':'Whole pool')}</div></div></div><div class="term-date">${ct!=='chargeable'?`<span class="charge-badge ${ct==='internal_school_use'?'internal':'foc'}">${ct==='internal_school_use'?'Internal school use':'Free of charge'}</span>`:(r.rate==null?'<span class="charge-badge">Rate not set</span>':`<strong>£${Number(r.rate).toFixed(2)}/hr</strong><div class="muted">${hrs.toFixed(hrs%1?2:0)} hr${hrs===1?'':'s'} · £${total.toFixed(2)} per session · ${r.vat_applicable?'VAT applies':'No VAT'}</div>`)}</div><div class="term-actions"><button class="link" onclick="editSession('${r.id}','${x.id}')">Edit</button> · <button class="link" onclick="delSession('${r.id}')">Delete</button></div></div>`}).join(''):'<div class="rb-empty">No weekly sessions added yet.</div>';let breakRows=breaks.length?breaks.map(r=>`<div class="term-row"><div class="term-label">${e(r.name||'Break')}<div class="muted">All sessions paused</div></div><div class="term-date">${e(ukDate(r.starts_on)+(r.ends_on&&r.ends_on!==r.starts_on?' → '+ukDate(r.ends_on):''))}</div><div class="term-actions"><button class="link" onclick="editBreak('${r.id}','${x.id}')">Edit</button> · <button class="link" onclick="delBreak('${r.id}')">Delete</button></div></div>`).join(''):'<div class="rb-empty">No breaks added.</div>';let exRows=ex.length?ex.map(r=>{let sess=RS.find(s=>s.id===r.session_id),label=sess?`${days[sess.day_of_week]} ${String(sess.start_time||'').slice(0,5)}–${String(sess.end_time||'').slice(0,5)}`:'Whole day';return `<div class="term-row"><div class="term-label">${e(label)}<div class="muted">Cancelled${r.notes?' — '+e(r.notes):''}</div></div><div class="term-date">${e(ukDate(r.exception_date))}</div><div class="term-actions"><button class="link" onclick="editCancellation('${r.id}','${x.id}')">Edit</button> · <button class="link" onclick="delCancellation('${r.id}')">Delete</button></div></div>`}).join(''):'<div class="rb-empty">No one-off cancellations.</div>';manage=`<div class="rb-manage"><div class="rb-manage-grid"><div class="rb-section"><div class="rb-section-head"><span>Weekly sessions</span><div class="actions"><button class="s" onclick="editMultiSession('${x.id}')">+ Add multiple days</button><button class="s" onclick="editSession(null,'${x.id}')">+ Add weekly session</button></div></div>${sessionRows}</div><div class="rb-section"><div class="rb-section-head"><span>Breaks</span><button class="s" onclick="editBreak(null,'${x.id}')">+ Add break</button></div>${breakRows}</div><div class="rb-section"><div class="rb-section-head"><span>Cancellations</span><button class="s" onclick="editCancellation(null,'${x.id}')">+ Cancel session / day</button></div>${exRows}</div></div></div>`}
return `<div class="rb-item"><div class="rb-summary"><div><div class="rb-name">${e(hn(x.hirer_id)||'School/Internal')}</div><div class="rb-sub">${e(x.name||'Recurring booking')}</div></div><div><b>${e(sn(x.site_id))}</b><div class="rb-sub">${shortUk(x.starts_on)} → ${shortUk(x.ends_on)}</div></div><div class="rb-counts"><span class="pill">${sessions.length} session${sessions.length===1?'':'s'}</span><span class="pill">${breaks.length} break${breaks.length===1?'':'s'}</span><span class="pill">${ex.length} cancellation${ex.length===1?'':'s'}</span><span class="rb-status ${active?'active':'archived'}">${active?'Active':'Archived'}</span></div><div class="rb-actions"><button class="p" onclick="manageProgramme('${x.id}')">${open?'Close':'Manage'}</button><button class="s" onclick="editProgramme('${x.id}')">Edit</button><button class="s" onclick="toggleProgrammeArchive('${x.id}',${active})">${active?'Archive':'Restore'}</button></div></div>${manage}</div>`}).join('')||'<div class="card">No recurring bookings match these filters.</div>'}
async function toggleProgrammeArchive(id,isActive){let{error}=await sb.from('recurring_programmes').update({active:!isActive}).eq('id',id);if(error)return alert(error.message);if(isActive&&OPEN_PROG===id)OPEN_PROG=null;await load()}
